<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 40px;
            background-color: #f8f9fa;
        }
        .result-card {
            max-width: 900px;
            margin: auto;
            padding: 30px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        pre {
            white-space: pre-wrap;
            word-break: break-word;
            font-family: 'Courier New', Courier, monospace;
            font-size: 16px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="result-card">
        <h2 class="text-center mb-4">🎯 Your Career Path Results</h2>

        <div class="mb-3">
            <p>Based on your answers and profile, here are some career paths that suit you:</p>
        </div>

        <pre><%= results %></pre>

        <div class="text-center mt-4">
            <a href="/career_quiz" class="btn btn-primary">🔁 Take Quiz Again</a>
        </div>
    </div>
</div>
</body>
</html>
